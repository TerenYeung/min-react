import React from './core/React';
import ReactDOM from './renderers/dom/ReactDOM';

var Chord = {
  React,
  ReactDOM,
};

window.Chord = Chord;
window.React = React;
window.ReactDOM = ReactDOM;

export default Chord;

